[project]
name = "chatapp"
version = "0.1.0"
description = "ChatApp monorepo"
requires-python = ">=3.12"
dependencies = [
    "google-genai>=2.6.0",
]

[tool.uv.workspace]
members = ["apps/api"]

[dependency-groups]
dev = ["ruff>=0.11.12"]
